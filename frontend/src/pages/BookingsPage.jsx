import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function BookingsPage() {
  return (
    <PagePlaceholder
      title='My bookings'
      description='Next: show upcoming, cancelled, and used tickets returned by /api/bookings/.'
    />
  );
}
