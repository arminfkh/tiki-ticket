import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function HomePage() {
  return (
    <PagePlaceholder
      title='Match ticket reservation'
      description='The React application is wired for routing, authentication state, and the Django API.'
    />
  );
}
