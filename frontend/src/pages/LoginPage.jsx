import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title='Log in'
      description='Next: connect the password and OTP login forms to src/api/auth.js.'
    />
  );
}
