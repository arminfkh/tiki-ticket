import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function ProfilePage() {
  return (
    <PagePlaceholder
      title='Profile'
      description='The backend exposes PATCH only here, so start from the user stored after authentication.'
    />
  );
}
