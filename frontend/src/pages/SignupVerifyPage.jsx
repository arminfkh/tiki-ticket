import PagePlaceholder from "../components/PagePlaceholder.jsx";

export default function SignupVerifyPage() {
  return (
    <PagePlaceholder
      title='Verify signup'
      description='Next: submit the email and OTP, then store the returned access token and user.'
    />
  );
}
